@model Elasticsearch_webapp.Models.query
@{
    ViewData["Title"] = "Home Page";
}


@using (Html.BeginForm("display","query"))
{
    <div class="form-group">
     @*   @Html.LabelFor(a => a.content, new { @class = "control-label col-md-2" })*@

        <h3>Enter your search query below</h3>
        <div class="col-md-10">
             @Html.EditorFor(a => a.content, new {htmlAttributes = new { @class = "form-control" } })
        </div>

    </div>

    <div class="form-control-static">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Submit" class="btn btn-primary" />
        </div>
    </div>

} 